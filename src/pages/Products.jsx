import Layout from '../components/layout';
// import data from '../data.json';

const Products = () => {
  return (
    <Layout>
      <h2>Products</h2>
    </Layout>
  );
};

export default Products;
